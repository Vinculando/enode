<p id="notice"><%= notice %></p>

<h1>postulations</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Publication</th>
      <th>Contenido</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @postulations.each do |postulation| %>
      <tr>
        <td><%= postulation.user %></td>
        <td><%= postulation.publication %></td>
        <td><%= postulation.contenido %></td>
        <td><%= link_to 'Show', publication_postulation %></td>
        <!-- <td><%= link_to 'Edit', edit_postulation_path(postulation) %></td>
        <td><%= link_to 'Destroy', postulation, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Application', new_publication_postulation_path %>
